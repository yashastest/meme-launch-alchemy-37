
# Solana network (devnet, testnet, mainnet-beta)
SOLANA_NETWORK=devnet

# Custom RPC endpoint (optional, will use public endpoint if not provided)
# RPC_ENDPOINT=https://api.devnet.solana.com

# Program ID (will be generated during deployment)
PROGRAM_ID=

# Treasury wallet address (platform fee recipient)
TREASURY_WALLET=

# Creator private key (base64 encoded) for token deployment
# CREATOR_PRIVATE_KEY=
